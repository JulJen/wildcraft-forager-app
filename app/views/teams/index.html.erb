<div>
  <div style="width: 500px;margin:auto">
    <img style="width: 500px" src="https://images.pexels.com/photos/1266005/pexels-photo-1266005.jpeg?auto=compress&cs=tinysrgb&h=650&w=940">
  </div>

    <div style="text-align: center;">
      <div style="display: inline-block; text-align: left;">
        <% if !@teams.nil? %>
        <h1>Current teams: <%= @user.teams.length %></h1>

        <% @user.teams.each do |team| %>
          <li>
          <%= link_to "#{team.name.capitalize}", team_path(team) %>
          </li>
        <% end %>
      <% else %>
        <h2><%= link_to "Create a team", new_user_team_path(current_user) %></h2>
      <% end %>
    </div>
  </div>
</div>
